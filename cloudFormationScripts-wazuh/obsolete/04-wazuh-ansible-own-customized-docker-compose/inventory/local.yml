---
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ playbook_dir }}/.venv/bin/python"
  
  children:
    wazuh_server:
      hosts:
        localhost:
